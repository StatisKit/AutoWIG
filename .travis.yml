os:
    - linux

language: python

python: "2.7"

before_install:
    - if [[ "$TRAVIS_OS_NAME" = "linux" ]]; then
        wget https://repo.continuum.io/miniconda/Miniconda2-latest-Linux-x86_64.sh -O miniconda.sh;
      elif [[ "$TRAVIS_OS_NAME" = "osx" ]]; then
        curl https://repo.continuum.io/miniconda/Miniconda2-latest-MacOSX-x86_64.sh -o miniconda.sh;
      fi
    - bash miniconda.sh -b -p $HOME/miniconda
    - export PATH=$HOME/miniconda/bin:$PATH
    - conda config --set always_yes yes --set changeps1 no
    - conda update -q conda
    - conda info -a
    - conda install conda-build
    - pip install coveralls


install:
    - conda build conda/python-autowig -c StatisKit -c salford_systems
    - conda install --use-local --no-deps python-autowig

script:
    - conda install python-clanglite -c StatisKit -c salford_systems
    - nosetests -v --with-coverage --cover-package=autowig

after_success:
    - coveralls

