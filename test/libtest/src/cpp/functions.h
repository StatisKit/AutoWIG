#ifndef FUNCTIONS_H
#define FUNCTIONS_H

double addition(const double& a, const double& b);

void duplicate(double& a);

double divide(const double& a, const double& b=2.);

int operate(int a, int b);

double operate(const double& a, const double& b);

#endif
