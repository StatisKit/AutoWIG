# -*-python-*-

import os

pj = os.path.join

SConsignFile()

env = Environment()
env.Append(CPPPATH = ['binomial'])
# Build stage
SConscript(pj("binomial/SConscript"), exports="env")

Default("build")
